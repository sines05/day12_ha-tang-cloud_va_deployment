import type { HolidayTheme } from '../holiday-config';
import defaultLogo from '../assets/images/head_logo.png';

const defaultTheme: HolidayTheme = {
  name: 'Default',
  logo: defaultLogo,
  decorations: [],
};

export default defaultTheme;
